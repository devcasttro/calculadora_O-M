from formula_calculo import calcular


tensao_saida_inversor = float(input('Qual a tensão de saída do inversor: '))
corrente_saida_inversor = float(input('Qual a corrente de saída do inversor: '))
capacidade_bateria = float(input('Qual a capacidade da bateria: '))
voltagem_bateria = float(input('Qual a voltagem da bateria: '))


print(tensao_saida_inversor, corrente_saida_inversor, capacidade_bateria, voltagem_bateria)